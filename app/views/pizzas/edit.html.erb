<%= turbo_frame_tag :modal do %>
  <div class="modal">
    <div data-controller="modals" data-action="turbo:submit-end->modals#submitEnd">
    <h1 class="font-bold text-4xl">Edit Pizza</h1>
      <%= render "form", pizza: @pizza %>
      <%= link_to "Cancel", pizzas_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    </div>
  </div>
<% end %>
